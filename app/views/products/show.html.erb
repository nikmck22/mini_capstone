
  <%= @product.name %>
  <%= @product.price %>
  <%= @product.description %>
  <img src="<%= @product.image_url %>">
  

<%# 
t.string "name"
    t.decimal "price", precision: 8, scale: 2
    t.string "image_url"
    t.text "description" %>